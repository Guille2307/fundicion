import { ImageSponsorPipe } from './imageSponsor.pipe';

describe('ImageSponsorPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageSponsorPipe();
    expect(pipe).toBeTruthy();
  });
});
