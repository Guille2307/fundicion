import { ImageSpeakerPipe } from './imageSpeaker.pipe';

describe('ImageSpeakerPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageSpeakerPipe();
    expect(pipe).toBeTruthy();
  });
});
