import { VirtualListIndexPipe } from './virtualListIndex.pipe';

describe('VirtualListIndexPipe', () => {
  it('create an instance', () => {
    const pipe = new VirtualListIndexPipe();
    expect(pipe).toBeTruthy();
  });
});
