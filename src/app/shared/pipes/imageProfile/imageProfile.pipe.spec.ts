import { ImageProfilePipe } from './imageProfile.pipe';

describe('ImageProfilePipe', () => {
  it('create an instance', () => {
    const pipe = new ImageProfilePipe();
    expect(pipe).toBeTruthy();
  });
});
