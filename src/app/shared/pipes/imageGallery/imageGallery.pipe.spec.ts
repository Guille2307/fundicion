import { ImageGalleryPipe } from './imageGallery.pipe';

describe('ImageGalleryPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageGalleryPipe();
    expect(pipe).toBeTruthy();
  });
});
