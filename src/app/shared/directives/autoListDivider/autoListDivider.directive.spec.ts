import { AutoListDividerDirective } from './autoListDivider.directive';

describe('AutoListDividerDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoListDividerDirective();
    expect(directive).toBeTruthy();
  });
});
